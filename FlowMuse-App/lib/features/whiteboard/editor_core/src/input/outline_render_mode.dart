/// Outline rendering mode (debug/test global A/B switch).
/// - polygon: existing addPolygon straight-line segments (baseline/control).
/// - quadratic: perfect-freehand official quadratic midpoint method (smooth curve).
enum OutlineRenderMode { polygon, quadratic }
