# R1 产品交互审查（第五轮·最终确认）

复核对象：`docs/研发记录/plans/2026-08-21-ai-visual-attachment-hybrid.md`（v5）
范围：R4-N1 修订验证（§1.2 表快捷指令行 StateError 列 + T6' 用例②）及其涟漪一致性。

## R4-N1 裁决：ADDRESSED

- §1.2 表（L45）StateError 列改为"移除活动槽附件（过期意图产物，与 null 分支同处理），`_error` 展示 + 追加后果说明"，并注明裁决理由；"与 null 分支同处理"含槽引用同步清空（L48 既有规则）。
- T6' 用例②（L126）同步："在途捕获失败→活动槽被移除、发送仍以纯文本完成且 `_error` 展示追加后果文案"。
- §7 第四轮记录（L183）留痕。

## 涟漪一致性检查（无新矛盾）

1. **重试路径**：失败已清槽 → 再次点击视觉快捷指令走 1.2.2 既有四象限（槽空未满额→加入并登记；槽空满额→内联提示不驱逐）。闭环，无未定义态。
2. **与 `_error` 展示的先后**：移除槽与写 `_error` 同属失败 handler 的两次 setState，表内顺序（先移除后展示）无依赖冲突；且该修订使 §1.2.5（L58）自洽——`_generate` await 在途捕获、异常吸收后以当前 `_attachments` 续发时，失败路径的 setState（已移除槽）先于 `_generate` 恢复（注册序），发送真正"以文字上下文为主"，后果文案不再失实。用例②正是此断言。
3. **"手动附件一律不动"不变量**：失败分支只动槽；1.2.2（L54）的普适条款覆盖两个分支，表为单一定义点，无缺口。
4. **开面板失败分支**（L44，槽尚未存在，无物可移）与**手动 chip 失败分支**（L46，槽与之无关、不动）均不受影响，无交叉矛盾。
5. **R2-Y1 涟漪**（C3 对 ai_agent_dialog.dart 触碰面收窄为仅增两个可选回调参数）：§2 whiteboard 行（L101）、T5'（L125）、提交切分（L129）三处表述一致，与 T6'/C4 的全面改写边界无冲突；属工程提交边界修订，产品规则不受影响。

## 最终判定

**可行**。第一至四轮全部发现（3C/5I/5M + 1C/3I/4M + 1I/3M + 1M）经 v2-v5 逐项闭合；本轮确认 R4-N1 修订到位且未引入新矛盾。未解决 Critical 0 / Important 0。产品交互规则（附件条、活动选区槽、null/失败三场景、满额四象限、隐私与失败文案、任务边界）自洽完备，可进入实施。
