diff --git a/.github/workflows/quality.yml b/.github/workflows/quality.yml
index ec5e634..6e5b416 100644
--- a/.github/workflows/quality.yml
+++ b/.github/workflows/quality.yml
@@ -53,6 +53,21 @@ jobs:
   server:
     name: Go test and vet
     runs-on: ubuntu-latest
+    services:
+      postgres:
+        image: postgres:17-alpine
+        env:
+          POSTGRES_USER: postgres
+          POSTGRES_PASSWORD: test-only
+          POSTGRES_DB: flowmuse_social_test
+        ports:
+          - 5432:5432
+        options: >-
+          --health-cmd "pg_isready -U postgres -d flowmuse_social_test"
+          --health-interval 5s --health-timeout 5s --health-retries 10
+    env:
+      FLOWMUSE_AUTH_TEST_DATABASE_URL: postgres://postgres:test-only@localhost:5432/flowmuse_social_test?sslmode=disable
+      FLOWMUSE_SOCIAL_TEST_DATABASE_URL: postgres://postgres:test-only@localhost:5432/flowmuse_social_test?sslmode=disable
     defaults:
       run:
         working-directory: FlowMuse-Server
