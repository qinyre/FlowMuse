# V3 语义成组构图：分阶段效果与部署记录

日期：2026-09-21。分支：`feature/v3-semantic-composition`。基线：`3356c9a`。
当前状态：A–D 实现与本计划的小规模验收完成，后端和平板 release 均已更新，PR #39 未合并。C 协议 `2bf9601`、消费 `126b5a2`、D 评分 `2b35d04`、真实模型与图片尺度修复 `ee1e42b` 分批提交。最终部署源代码为 `ee1e42b`，其后的交付提交只补测试导出、历史对比入口及本记录。下方保留 `5601f36` 的 A+B 历史记录，不与最终结果混用。

## C/D 最终离线检查

- 评分改为实际 reduced Scene + render snapshot；输入侧冻结关系分母，缺测/不适用分开，原稿只读比较。完整可比较且改善至少 0.05 才默认推荐，否则默认保留原样；备选可手选，没有伪造的 no-op patch。
- 全量 Flutter：1753 通过、3 条条件跳过（已有环境测试、显式开启的在线验收、仅供隔离旧版本执行的对比入口）。12 页效果集反复生成确定，集中测试共 23 项通过。缺资产页不编造改善差值，已排好页默认保留；伪造 placement 数据不能改变实际指标。Go 全量 test/vet 通过。
- 冻结 `composition-score/2` 与 `semantic-composition/2` 构图策略；后者不是语义协议版本（语义协议仍为 `/1`）。
- 同机 Excalifont、一次预热十次本地候选（不含网络/构造样例）：最终耗时微秒 `[41641,43048,40753,39971,39716,39530,37080,37905,37394,36847]`，中位数 39.623ms、最大 43.048ms；相对 D 前 30.806/35.890ms，增加约 8.817/7.158ms（包含原稿额外只读渲染）。未据十次样本宣称 p95；这不是设备端或识别网络耗时。

### 12 页看图口径

数据为 `composition/fixtures/semantic_composition/pages.dart`，8 开发 + 4 留出；真值角色/关系是输入，不是模型自评。插画使用不同资产标记，但不是动物/植物识别素材。逐页查看 before/after，按内容保留、组织关系、阅读层级、对齐间距、可读性记录；不是依据分数反推美观。无按 fixture ID 的生产分支。

| 页 | 默认结果与视觉结论 |
| --- | --- |
| 01 双图双说明 | peerGrid；两图顶齐、说明各自邻接，标题清楚，短笔记下方留白合理 |
| 02 交叉摆放 | peerGrid；按预给关系复原两组，同级并列，没有把空间邻近当真值 |
| 03 单图长正文图注 | single；图注与图邻接、长段落完整可读，末行短字仍可能出现，不宣称精细字距排版 |
| 04 两图共同说明 | single；两图后只出现一份说明，组织正确但整体较纵长，属于可用而非最佳杂志构图 |
| 05 章节与列表 | single；标题依附各自章节、编号连续，组间距清楚 |
| 06 软/硬换行 | single；CJK 软行连接、Latin 词边界保留空格，原生硬换行仍在；原稿用模拟笔迹，此页只验投影机制，不证明手写识别视觉质量 |
| 07 缺资产与固定对象 | 默认原稿；缺图和固定框/箭头不隐藏，安全备选可预览但没有完整改善结论 |
| 08 已排好 | 默认原稿；真实二次比较差值约 0，不为制造修改而重排 |
| 09 留出：换主题与位置 | peerGrid；两组完整且顶齐，标题/说明清晰 |
| 10 留出：横竖三组 | mediaSide；三行对应清楚，纵横图原裁剪保留，无新遮挡 |
| 11 留出：讲义正文列表配图 | single；长文、列表、配图与尾段完整，阅读顺序明确 |
| 12 留出：图形箭头与保留对象 | mediaSide；安全图文整理，右下原框与箭头不动 |

真实字体导图命令同下；完整 PNG 仍留本机 build，不批量提交构建产物。原稿笔迹尺度以区域高度/物理行数估算，不能视为与原生字体完全等价的测量。

### 与旧算法的独立对照

另在 `3356c9a` 的隔离 checkout 运行常驻 `semantic_composition_baseline_test.dart`，输入同一批导出的 12 页 Scene 与 `pages.dart` 真值。使用同一微软雅黑文件、页尺寸与资产，统一补全页归属以隔离构图效果；旧链直接调用该提交的 `runFromSemanticAssembly`，不在新生产代码中复制旧算法。旧协议的一文一图上限如实保留，共同说明只给第一图关联，不复制文字。此对比不评 OCR，也不把 A 的范围修复算作构图收益。

旧结果 01–06、09–11 默认 `conservativeLayout`；08 无候选、保留原稿；07/12 在原生闭包组装时报重复 sourceRefs，记录实际失败并导出原稿，不将导出测试的退出码 0 当成旧算法效果通过。对应日志/12 张旧图留在本机 `build/semantic-composition-legacy-evidence/`。

下表是执行代理逐页看图判断，不是算法分数或独立人工评审。五项顺序为「归属、阅读路径、字号层级、对齐间距、占用留白」；06 无图，归属不适用。新默认与输入原稿对照见上表，以下才是与旧算法对照。

| 页 | 旧版五项 | 新版五项 | 比较理由 |
| --- | --- | --- | --- |
| 01 | 好/好/可用/可用/差 | 好/好/好/好/好 | 明显改善；两组并列、图不再挤在左上角，标题层级清楚 |
| 02 | 好/好/可用/可用/差 | 好/好/好/好/好 | 明显改善；组序正确且同级顶齐，尺寸与页宽协调 |
| 03 | 好/好/可用/可用/差 | 好/好/好/好/可用 | 明显改善；长文和图片可读尺度统一，末行单字仍可优化 |
| 04 | 可用/可用/可用/可用/差 | 好/好/好/好/可用 | 明显改善；共同说明不再夹在两图之间，但整体仍偏纵长 |
| 05 | 好/好/可用/可用/差 | 好/好/好/好/好 | 明显改善；章节节奏、列表间距和图片尺度更清楚 |
| 06 | 不适用/可用/可用/可用/可用 | 不适用/好/好/好/可用 | 明显改善；去掉已批准的碎行，主动硬换行保留 |
| 07 | 差/可用/可用/差/可用 | 差/可用/可用/差/可用 | 不退化；缺资产不强改，默认仍是原稿，可手选安全部分整理 |
| 08 | 好/好/好/好/好 | 好/好/好/好/好 | 不退化；默认保持已排好的页面 |
| 09 | 好/好/可用/可用/差 | 好/好/好/好/好 | 明显改善；更换文案与位置仍有统一并列构图 |
| 10 | 好/可用/可用/可用/差 | 好/好/好/可用/可用 | 明显改善；三组图文对应更直接，横竖图不同宽度并非完美齐列 |
| 11 | 好/好/可用/可用/差 | 好/好/好/好/可用 | 明显改善；讲义字号与配图适合页尺度，段尾孤字仍可能出现 |
| 12 | 差/可用/可用/差/可用 | 好/好/好/好/可用 | 明显改善；图文安全整理，右下框/箭头原位不动，留白不强填 |

开发集 6/8 明显改善、2/8 不退化；留出集 4/4 改善。核心页五项均为好，达到本计划的小规模门槛，不外推为普遍美观保证。挑选一对合成图入库：[旧默认](cd-core-old.png)、[新默认](cd-core-new.png)。

复跑旧版：在隔离的 `3356c9a` checkout 复制上述常驻测试及 `fixtures/semantic_composition/pages.dart`，执行 `flutter pub get` 后运行：

```powershell
flutter test --no-pub "--dart-define=LAYOUT_BASELINE_INPUT_DIR=<当前版本导出目录绝对路径>" "--dart-define=LAYOUT_EVIDENCE_CJK_FONT=C:/Windows/Fonts/msyh.ttc" test/features/whiteboard/smart_layout/composition/semantic_composition_baseline_test.dart --reporter failures-only
```

隔离副本与生成插件副产物已清理，当前分支不保留第三套旧生产链路。

## C/D 最终部署与实机验收

### 部署和本轮修复

- 后端以本分支识别包升级 `/opt/flowmuse/source/FlowMuse-Server`，只重建应用服务；未改 `.env`、数据库或存储卷。最终镜像 SHA：`186df0a64fb1e638e679add5c6bac2892e4a29c2d2a9346912f0e05ebe7308ee`，`/health` 为 `ok`。旧镜像保留 `flowmuse-server-collab-server:pre-composition-126b5a2`，旧识别源码备份 `/tmp/recognition-pre-composition-126b5a2.tar.gz`。
- OPD2404 / Android 16 API 36 / arm64；`ee1e42b` release 覆盖安装成功，不卸载、不清数据。沿用 `http://124.221.236.179:48931`；APK SHA-256：`c95fb60207e16f55b786429a2fdc771447d985916111b4ff952d3844a761aebd`。字体为应用 Excalifont 与设备 CJK 回退，不把桌面字体截图冒充实机。
- 初次在线验证出现真实失败：纯图注被 provider 放入空 `textUnitIds` 的 mediaGroup；已明确纯图注只走 captions，补可定位规则码。交叉样例初测错误同时暴露测试概览字体方块，已加载真实字体/Excalifont 编号，提示先辨图像主体再匹配文字，不能只按距离。
- 实机单栏优先还暴露了二次缩窄：双列中的图片又乘 0.6，空隙过大且影响真实密度得分。改为遵守页面级图片上限但允许填满列；短同级图注的并列判断不再完全依赖 provider 的 pageIntent，长文不强塞双列。不是单纯抬高某个模板分数。
- 长文测试文件初次导入会自动撑宽，导致长段落越出页面；修正常驻导出入口 `autoResize: false` 后重新导入，确认 4 个源均参与，首次只有 3 个源的试跑不计通过。

### 真实模型：冻结修复后的完整复跑

入口 `semantic_composition_online_test.dart`，显式服务地址 + 本地原猫狗笔记；使用真实照片、真实 HTTP/provider，不应用或更改源画布。crossed/shared 是由原照片构建的两个独立原生文字页面。每个模型输出关系都逐条与独立真值比对，未关联计漏配；此前开发失败不计入此冻结批次，也不隐瞒。

| 页面/运行 | 正确/输出/应有关联 | read + structure 调用数 | 识别链耗时 | 含本地候选总耗时 | 默认 |
| --- | --- | --- | --- | --- | --- |
| 原始手写 core-1 | 2/2/2 | 1+1 | 25.235s | 25.870s | peerGrid |
| 原始手写 core-2 | 2/2/2 | 1+1 | 25.647s | 26.178s | peerGrid |
| 原始手写 core-3 | 2/2/2 | 1+1 | 25.768s | 26.390s | peerGrid |
| 图片与文字交叉摆放 | 2/2/2 | 0+1 | 12.157s | 12.648s | peerGrid |
| 两图共一段说明 | 2/2/2 | 0+1 | 11.869s | 12.195s | mediaSide，正文一次 |

核心页三次均包含两处软换行提示并得到正确配对；合计 10/10 正确、0 错配、0 漏配，本小样本 precision/recall 均为 100%。这只是三种页面、五次运行，不是总体准确率，也不是五次平板测试。切候选/评分不增加模型调用。

复跑命令（本地原笔记不入库）：

```powershell
flutter test --no-pub --dart-define=COMPOSITION_TEST_SERVER=http://124.221.236.179:48931 --dart-define=COMPOSITION_TEST_NOTE=build/semantic-composition-catdog-original.excalidraw "--dart-define=LAYOUT_EVIDENCE_CJK_FONT=C:/Windows/Fonts/msyh.ttc" test/features/whiteboard/smart_layout/composition/semantic_composition_online_test.dart --reporter failures-only
```

### 平板实际操作

| 页面 | 预览/应用 | 撤销与重开 |
| --- | --- | --- |
| 用户原猫狗页 | 默认同级图文并列；标题清楚，两条说明不再保留碎行、分别对应正确照片，两图顶齐；默认应用后与预览一致 | 应用→撤销→重做→撤销→退出重开→再导出；130 个原活动元素的已有字段逐项一致，两张图片 dataURL 一致。仅导出资源 created 字段变化，不声称 JSON 字节全等 |
| 单图长文与图注 | 4 个源参与、默认单栏，长段落完整、图注邻接；应用后未变色或丢正文 | 撤销、退出重开后恢复原位置与长正文；保留命名测试笔记 `V3-CD-long` |
| 已经排好页 | 默认保留原样、应用按钮禁用，原稿可读；手选同级并列备选后才能应用，实际一致 | 手选应用→撤销→退出重开正常；保留命名测试笔记 `V3-CD-organized` |

服务端实机阶段日志：猫狗 read 7.869s、structure 20.719s（结构发送 4.004s、等待 16.710s）；长文 structure 4.736s；已排好页 structure 6.451s。主要等待仍来自 provider/传输，没有宣称本轮做到秒级整页识别。release 未取得可靠客户端阶段日志，因此不编造平板端本地候选耗时。

当前设备回到猫狗原稿。两条临时试导入/空白笔记移入可恢复回收站，原用户笔记未删除；未采用清应用数据操作。测试中一次绕过文件选择器的无授权 content URI 注入被 Android 拒绝并触发原生异常，改用正常“打开→文件选择器”后完成导入；不将该无效注入算布局失败，也不宣称应用不存在其他导入问题。

本机最终证据：`build/flowmuse-cd-default-final.png`、`flowmuse-cd-applied-final.png`、`flowmuse-cd-{undo,redo}-final.png`、`flowmuse-cd-long-{preview,applied,restored}.png`、`flowmuse-cd-organized-{review,applied,restored}.png`；原稿/恢复导出 `semantic-composition-catdog-{original,cd-restored}.excalidraw`。用户笔记、照片、模型响应和 APK 只留本机 build，不上传 PR。

## 已实现

- A：V3 共用页面范围、固定内容保护、真实物化内容/图文关系/阅读序检查、过期候选拒绝，以及应用时不被当前工具默认样式覆盖。
- B：先组内真实测量，再整页流式放置；图上文下、左右图文、同级组两列；统一字号/行长/间距、实际图片像素限制、跨家族去重、放不下时整组保留。沿用现有 reducer、renderer、源账本与撤销链，无新增模型调用。
- C：同一 structure 请求协商章节、多图共同说明与 OCR 软换行；语义文档只存一份 composition 关系，shared text 不复制。测量/物化共用带原文指纹的显示投影；原生硬换行不变。关系纠错/角色变化/整组保留可逆，纯语义重排不重新调用模型、不作废图片资产。同级图片顶对齐、前置说明底对齐，不跨章节并列。
- D：基于最终渲染事实评分、只读原稿比较、默认保留/解释原因，完成 12 页新旧看图、真实 provider 五次和三类平板操作。边界与失败过程见上，未增加模型审美循环或逐任务复审流程。

### C 离线检查点

- 服务端全量 `go test ./...`、`go vet ./...` 通过（协议批次）。
- 客户端全量 1740 通过、1 个既有跳过；静态分析零问题。共享一文多图、纠错零模型调用、原文/软换行回滚、整组保留与逆操作、真实同级图顶对齐已补回归。
- D 前同机字体基准：Excalifont；现有集成测试 `RUN_LAYOUT_BENCHMARK=true`，一次预热后十次，本地生成+物化+门禁，不含网络/样例构建。耗时微秒 `[35890,33947,31351,30153,31735,30007,28804,30261,31853,29148]`，中位数 30.806ms、最大 35.890ms。D 完成后用同一入口复跑；不是线上时延。

## A+B 历史验证结果（5601f36，非最终统计）

以下在 `FlowMuse-App/` 执行，退出码均为 0：

```powershell
flutter analyze --no-pub
flutter test --no-pub --reporter failures-only
flutter test --no-pub --dart-define=EXPORT_LAYOUT_EVIDENCE=true "--dart-define=LAYOUT_EVIDENCE_CJK_FONT=C:\Windows\Fonts\msyh.ttc" test/features/whiteboard/smart_layout/composition/semantic_composition_integration_test.dart --reporter failures-only
```

- 静态分析零问题；全量 1733 项通过、1 项原有跳过。
- 集中集成测试 9 项通过：双图三种构图/重复运行确定性/实际门禁/应用撤销重做；OCR、裁剪与强调色；低像素图；文字在图片前的阅读序；损坏资产；连续标题避障；超长内容整组保留；破坏最终位置的拒绝负例；原生闭包别名与输出映射。
- 既有长文测试改为新页面尺度下的 32 字行长上限，仍检查真实换行；极短页审阅测试改为只提供一个真实方案，不再要求重复候选。多候选切换及键盘选择仍由既有 session view 测试覆盖。
- 测试日志和完整导图位于忽略的 `build/`；仅挑选两张不含用户内容的合成 fixture 对照图入库供 PR 审阅，不提交其他构建副产物。

## A+B 历史看图记录与证据边界

源 fixture 在 `test/features/whiteboard/smart_layout/composition/semantic_composition_integration_test.dart`，由真实原生文字、可选手写替代源、两幅合成插画构成。角色/关系独立给定，不是模型输出；不证明线上模型能识别猫狗，也不是用户原笔记复现。

导图使用真实 `DraftSceneRenderer`，字号测量与渲染均载入本机微软雅黑为 `LayoutEvidenceCJK`。测试导出时仅加纸色底，未移动截图内元素。默认 CI 不依赖本机字体；Windows 字体的效果不冒充 Android/OHOS 实机字体。

本机输出（可通过上述命令重建）：

| 文件（相对 `FlowMuse-App/build/semantic-composition-evidence/`） | 看图结论 |
| --- | --- |
| `01-original.png` | 两幅图很小且与说明远离，标题与正文缺少共同对齐线 |
| `01-single.png` | 标题、两幅图和对应说明按完整组顺序排列，比例/文字/强调色保留；但图片在短说明场景占比偏大，不能据此断言它该排第一 |
| `01-mediaSide.png` | 两组左右图文关系明确，同级图片等宽、段首对齐；标题层级和组间距清楚，下半页允许留白 |
| `01-peerGrid.png` | 两个同级图文组并列，文字各自在对应图下；适合短说明，页面较稀疏。是否优先推荐应由 D 的实际占用评价决定 |

以上是原稿与新构图对照，**没有**完成旧版候选算法与新默认第一候选的完整对照评测。B 的局部可编辑构图已验证；当前软评分仍是旧算法，不把“有一张合理候选”记成“默认推荐已验收”。

PR 对照图（均为离线合成样例，不是平板或真实 provider 证据）：[输入原稿](offline-original.png)、[同级图文并列](offline-peer-grid.png)。

## 2026-09-21 A+B 原猫狗笔记历史实机测试

### 版本与部署

- 设备：OPD2404，Android 16 / API 36，arm64，release 包；源提交 `5601f36`。APK SHA-256：`8cf958c3ecc5e70523f0b667065fbce28293fe96bf6157bcedb98c7b089517fa`。
- 构建：`flutter build apk --release --no-pub --target-platform android-arm64 --dart-define=FLOWMUSE_COLLAB_SERVER_URL=http://124.221.236.179:48931`，成功；`adb install -r` 成功，不卸载、不清数据。沿用已安装旧包的实际服务地址，而非无法连通的 443 域名端口。
- 服务端：运行目录不是 Git checkout；逐文件比较 55 个 Go 源码/依赖文件（仅统一 CRLF/LF）与当前分支完全一致，`/health` 返回 `ok`，本次没有需要部署的后端差异，因此未重启容器、未改配置或数据库。运行镜像 SHA 前缀 `d995dba9f859`；真实 provider 为 `doubao-seed-2-1-turbo-260628`。
- 更新前保留旧 APK，并通过系统“分享 .excalidraw → 保存到文件管理”导出原稿。备份与实机截图仅保存在本机 `FlowMuse-App/build/`；不将用户笔记 JSON、图片资产或 APK 随 PR 上传。

### 实测结果

| 检查 | 真实结果 |
| --- | --- |
| 原稿范围 | 130 个活动元素：127 条笔迹、2 张图片、1 个页面框；猫图缺少 `customData.flowMuse.pageId`，狗图明确为 `page-1`，两图均在该唯一页面内且未锁定 |
| 旧漏图原因 | 原范围的精确 pageId 过滤会排除猫图；本轮真实元数据支持该原因，不再只是推测。新链临时纳入缺归属图片，没有回写原始 pageId |
| 本轮识别 | 真实在线请求 1 次 read + 1 次 structure；得到标题 41 源、图注 40 源、狗图 1 源、图注 46 源、猫图 1 源，共 129 源、5 块，0 保留；页面框不算识别源 |
| 关联与候选 | 本页两组说明均与正确图片相邻，没有遗漏或串图；提供单栏与同级图文并列 2 个候选。说明本轮被识别为 caption，不是 body，因此未生成左右正文轨道候选 |
| 预览与应用 | 选择并列候选应用成功；实际标题、文字颜色、字号、图文顺序和相对位置与预览一致；未复现旧工具默认样式覆盖。画布初始 100% 放大，缩小后可看完整页面 |
| 撤销与重开 | 应用→单次撤销→重做→再撤销→退出笔记→重新打开均执行，最终留在原稿。重新导出后，130 个原活动元素的已有字段逐项相同，2 张图片 dataURL 相同；资源 `created` 导出字段变化，不宣称整个 JSON 逐字相同 |

仅此一次真实页面运行，不能用“两组正确”宣称达到计划的多页 precision/recall 或重复 3 次验收要求。

客户端日志：识别链 `total_ms=24066`，`model_calls=2`；本地候选 `elapsed_ms=516`。服务端 read 为 6.680 秒；structure 约 15.892 秒，其中发送约 3.655 秒、等响应约 12.230 秒，结构请求约 1.70 MB。这里都是单样本耗时，不是 p95；主要时间仍在模型/请求传输，而非本地排版。

### 效果尚未达标的部分

1. 默认仍推荐单栏；本页单栏图片偏大、预览缩到适合内容后字偏小，并列更易对照。这验证了 D 的评分/推荐改造仍有必要。
2. 两段说明保留了原手写的 2/3 行碎行，没有利用可用宽度重排；猫说明末字独占一行，C 的软换行消费与角色边界需结合 caption 实例核对，不能只测试普通正文。
3. 并列两组的文字顶部一致，但因说明行数不同，图片顶部不一致；还需在组内布局/效果收口中解决同级媒体对齐，不能只靠分数调整。

本机截图：`build/flowmuse-ab-review.png`（默认单栏）、`build/flowmuse-ab-peer-grid-clean.png`（并列预览）、`build/flowmuse-ab-applied-readable.png`（应用后）、`build/flowmuse-ab-redone.png`、`build/flowmuse-ab-final-original.png`（最终原稿）。原始及恢复后导出：`build/semantic-composition-catdog-{original,restored}.excalidraw`。

结论：**漏图/图文失散、应用不一致及内容恢复在本页得到实际验证；“默认即达到预期排版效果”没有通过，不能提前宣告 C/D 或整体效果完成。**

## 最终结论与保留边界

A+B 历史遗留的默认单栏、图注碎行、同级图片顶部不齐已在 C/D 收口，并复跑真实模型及平板确认。本计划实现与限定样例验收完成；PR #39 仍待合并，不等于已进入 main。

仍不承诺任意复杂笔记都达到杂志级效果：多图共同说明的纵长构图、段尾孤字、复杂页面更广泛的模型误配率属于后续质量优化。OCR/结构网络仍约十几到几十秒；没有增加额外审美模型调用，也没有用小样本声称普遍 100% 或 p95 达标。
