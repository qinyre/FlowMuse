# Sprint 2 AI 反思日志

更新时间：2026-07-14

记录格式：每条按 **问了什么 → AI 给了什么 → 我们改了什么 → 验证证据** 四段组织。验证证据用于说明人工判断已经通过测试或真机反馈闭环。

---

## 安卓书写优化

- **问了什么**：为什么不跟笔，画圆为什么有甩尾和折角？
- **AI 给了什么**：输入采样、预测点、平滑曲线和渲染路径分析。
- **我们改了什么**：分 P0/P1 实施；旧路径效果不佳后隔离新输入模型，并保留可调参数。
- **验证证据**：压感、轮廓、输入模型单测；安卓真机反馈。

## 实时协作笔迹

- **问了什么**：如何从“抬笔后同步”改为笔画过程同步？
- **AI 给了什么**：增量元素广播、发送合并和最终帧保证方案。
- **我们改了什么**：未采用全场景高频上传；改为发送实时笔迹帧，阻塞时只保留最新中间帧，最终帧必须送达。
- **验证证据**：`collaboration_repository_sync_test.dart`。

## 协作热路径优化

- **问了什么**：大文件双方同时编辑为什么卡顿和丢同步？
- **AI 给了什么**：拆分本地保存、封面和协作调度，引入 ChangeAccumulator 与接收合并窗口。
- **我们改了什么**：排除未经验证的“二进制膨胀 6 倍”等假设，改为先采集指标、再分阶段实施。
- **验证证据**：accumulator 单测；200 元素增量广播测试。

## PDF 导入持久化

- **问了什么**：多页 PDF 第二次打开时图片为什么消失？
- **AI 给了什么**：场景内容、图片引用和平台存储差异分析。
- **我们改了什么**：未大改数据库；修正大场景内容存取路径，并保留 Android/鸿蒙适配边界。
- **验证证据**：PDF 导入与场景仓库大内容回归测试。

## 系统分享与文件打开

- **问了什么**：`.markdraw/.excalidraw` 如何分享并让系统“打开方式”识别？
- **AI 给了什么**：Android Intent、鸿蒙 Want/Skills、入站队列和文件名传递设计。
- **我们改了什么**：分平台适配，公共层只接收标准载荷；补充后台唤醒和文件标题传递。
- **验证证据**：分享载荷、Android/鸿蒙服务与外部文档入站测试。

## 鸿蒙协作兼容

- **问了什么**：创建房间和账号区域为什么报 `unsupported_platform`？
- **AI 给了什么**：定位安全存储、HTTP 和平台插件缺失路径。
- **我们改了什么**：将平台能力收敛到适配层，公共协作状态机保持一致。
- **验证证据**：鸿蒙真机创建/加入房间；协作配置测试。

## 协作优化方案选型

- **问了什么**：三份 AI 研究文档（claude/codex/zcode-best）哪份最好？
- **AI 给了什么**：逐项对比 LWW/CRDT、ACK 机制、CSR、压缩等关键技术点。
- **我们改了什么**：选定 codex-best 为主线，吸纳 zcode 性能项与 claude 运行环境补强。
- **验证证据**：`socket_io_client 3.1.6` 源码验证 CSR 已支持 pid/offset；`sendBytes` 二进制传输。

## Phase 0 热路径修复

- **问了什么**：计划能否实现 codex-best 第一阶段？关键隐患在哪？
- **AI 给了什么**：审计出 5 个阻塞问题：syncAll→sceneInit、undo/redo 未写回、签名未传播、version 表缺 nonce、快照未等 in-flight。
- **我们改了什么**：逐项修复后由 Claude Code 执行，再审计确认 5 项全部闭合。
- **验证证据**：12 个提交、accumulator/reconciler/发送串行化单测全过。

## 安卓创建房间超时

- **问了什么**：安卓创建协作房间报 `TimeoutException after 0:00:15`，鸿蒙正常，为什么？
- **AI 给了什么**：排查 `encrypted_scene_store.dart` 的 15s 超时、`http.Client` 双端一致、`usesCleartextTraffic` 已设。
- **我们改了什么**：定位为网络层问题（端口 48931 在安卓端网络被封锁），非代码缺陷；建议改端口或测试浏览器可达性。
- **验证证据**：鸿蒙正常；代码无 Phase 0 改动；待安卓浏览器/蜂窝网络验证。

## 编辑器设置

- **问了什么**：哪些工具、手势、手写笔和文档设置最容易落地？
- **AI 给了什么**：复用控制器状态和 `LocalSettingsRepository` 的分批方案。
- **我们改了什么**：先实现已有开关和状态记忆，不引入新的状态管理库。
- **验证证据**：偏好 JSON、安全默认值与控制器应用测试。

## 工具栏导入图片

- **问了什么**：点「导入图片」为什么先弹「文件管理/相册」选择来源？
- **AI 给了什么**：定位到鸿蒙 `DocumentViewPicker` 会列出来源、安卓 `file_picker` 走 `ACTION_PICK`。
- **我们改了什么**：鸿蒙改用 `PhotoViewPicker`（`@kit.MediaLibraryKit`）、安卓改用 `Activity.startActivityForResult` + Photo Picker，双端直接进相册。
- **验证证据**：鸿蒙/安卓真机导入图片直进相册；`flutter analyze` 通过。

## 设置页未实现功能

- **问了什么**：设置页哪些分区是占位、实现优先级怎么排？
- **AI 给了什么**：按“成本×价值”分档，识别 `?section=` 路由参数未消费等隐性缺陷。
- **我们改了什么**：先落地第一档（其他/隐私/帮助反馈/路由修复），网盘备份先选型后再做。
- **验证证据**：设置页四个分区内容填充；`?section=` 路由参数生效；`flutter analyze` 通过。

## 网盘备份选型

- **问了什么**：百度网盘 vs 坚果云 vs 自建，哪条路最省事？
- **AI 给了什么**：对比 OAuth 准入门槛、回调复杂度、依赖和代码量。
- **我们改了什么**：否决百度网盘（OAuth 审核与移动端回调不确定）；选定坚果云 WebDAV（零依赖、复用 `http.Client`、复用加密）。
- **验证证据**：选型结论待验证——申请 AppKey/跑通 OAuth 前不写代码。

## 房主退出限制

- **问了什么**：为什么房主单独「退出房间」会让协作房间残留？
- **AI 给了什么**：梳理 leave（仅断开）与 end（服务器标记 ended）的行为差异。
- **我们改了什么**：取消房主单独退出选项——菜单对房主隐藏「退出房间」，返回键直接走「结束协作」。
- **验证证据**：协作菜单房主只见「结束协作」；`flutter analyze` 通过。

## 全局识别多模态 OCR

- **问了什么**：手写识别为什么只支持文字、不支持图形/公式？
- **AI 给了什么**：分析服务端识别管线与前端进度展示缺口。
- **我们改了什么**：改造为多模态 OCR；补全局识别进度显示；修复识别后属性面板排序崩溃。
- **验证证据**：全局识别进度条真机；属性面板排序回归测试。

## 属性面板与工具栏

- **问了什么**：属性面板不可收起、工具栏位置固定、展开入口缺失怎么统一解决？
- **AI 给了什么**：梳理面板/工具栏的状态机与布局约束。
- **我们改了什么**：工具栏支持收起/展开与位置切换；属性面板加展开入口并修复收起逻辑；优化创建笔记时画布过大。
- **验证证据**：真机交互反馈；多分辨率布局回归。

## Web 白屏与跨域

- **问了什么**：Web 启动白屏、加入房间 `Failed to fetch` 的原因是什么？
- **AI 给了什么**：定位 PDF.js、Web SQLite 和 REST CORS 三条独立问题。
- **我们改了什么**：使用现有插件的官方初始化方式；REST 统一增加 CORS；未将中间件部署到线上。
- **验证证据**：Web debug/release 构建、SQLite 重启与 CORS 单测。

## 项目架构与白板内核接入

- **问了什么**：FlowMuse 客户端和服务端应该如何拆分，白板编辑器内核怎样接入才不会和资料库、协作层互相污染？
- **AI 给了什么**：建议按客户端、服务端、文档分层组织工程，并把白板能力收敛到编辑器内核、Repository、ViewModel 和页面壳之间的清晰边界。
- **我改了什么**：整理 FlowMuse 工程层级，搭建 Flutter 客户端与 Go 协作服务端基础结构；抽取应用壳、侧边栏、右侧页面骨架和主题系统；用新的编辑器内核替换旧白板画布入口，并保留场景内容、元素字段和控制器状态的统一流转。
- **验证证据**：首屏资料库、白板入口、主题切换和侧边栏交互可运行；白板场景能在资料库笔记中创建、打开、保存和重新进入。

## 协作共享需求兼容数据模型

- **问了什么**：白板数据模型如何对齐协作共享需求，哪些旧字段和旧负载应该移除？
- **AI 给了什么**：梳理场景 JSON、元素字段、版本语义、删除墓碑、顺序字段和协作快照之间的关系，指出新旧格式混用会导致同步和导入导出不可控。
- **我改了什么**：收敛白板场景格式，移除旧协作负载、旧元素结构和旧视口兼容路径；补充架构约束文档，要求后续 Frame、图片、绑定箭头、分组和导入导出继续以协作共享需求兼容语义为准。
- **验证证据**：白板场景序列化与恢复路径可用；后续 PDF 导入、实时协作和外部 `.markdraw/.excalidraw` 入站都复用同一场景语义。

## 实时协作主链路

- **问了什么**：如何从单机白板扩展到多设备协作，同时保证房间创建、加入、快照同步、退出和房主结束流程一致？
- **AI 给了什么**：拆解协作 Repository、传输层、加密存储、房间生命周期和前端状态展示的职责，并提示创建房间、加入房间、实时元素广播和最终快照要分别验证。
- **我改了什么**：实现白板客户端协作协议，接入 Go 服务端和协作数据格式；完成房间创建、加入入口、快照载荷对齐、实时帧转发、图片同步、退出与房主结束流程；补充 Docker 部署配置和多设备协作部署说明。
- **验证证据**：多设备协作可创建和加入房间；鸿蒙真机可完成创建/加入；实时笔迹、图片同步和房主结束逻辑经过真机反馈与协作相关测试回归。

## 生命周期与稳定性治理

- **问了什么**：白板切换、文本编辑器、浮层菜单和协作状态为什么容易触发布局重入或已销毁上下文断言？
- **AI 给了什么**：建议把 UI 时机问题从单点修补改为统一生命周期调度，区分当前帧、下一帧、上下文稳定后和组件仍挂载四类条件。
- **我改了什么**：统一应用弹层和白板协作生命周期调度，修复白板场景切换、内联文本编辑器、字体面板、自定义浮层、菜单浮层和结束协作失败提示等路径；明确弹层输入控制器所有权，减少组件销毁后的异步回调。
- **验证证据**：白板切换、文本编辑、菜单打开/关闭和结束协作流程不再复现原有断言；相关交互在后续工具栏、属性面板和设置页迭代中继续复用同一调度方式。

## 资料库、本地存储与账户体系

- **问了什么**：资料库怎样从演示数据切到真实本地数据，同时支持笔记本、标签、账户身份和协作昵称？
- **AI 给了什么**：建议以 Repository 和 Riverpod 聚合状态作为单一数据源，避免页面各自维护笔记、笔记本、标签和账户显示状态。
- **我改了什么**：真实化客户端首页数据，统一笔记与笔记本语义；实现本地数据库中的笔记本和标签系统，修复 Android 本地数据库初始化问题；完善账户系统、匿名昵称和头像显示，并优化侧边栏回收站分割线与资料库页面密度。
- **验证证据**：资料库首页、笔记本页、标签页、账户区域和协作身份展示可联动；Android 本地数据库初始化问题修复后，笔记与集合数据可正常创建和读取。

## AI 识别笔与服务端识别链路

- **问了什么**：识别笔主链路如何接入服务端，MyScript 批量识别和公式识别为什么不稳定？
- **AI 给了什么**：定位前端采样、识别笔标记、服务端环境变量、MyScript 请求格式和公式识别接口之间的断点。
- **我改了什么**：实现识别笔字迹识别，配置服务端本地识别环境变量；修正 MyScript 批量识别请求和公式识别请求；传递识别笔采样时间，避免识别笔标记被页面数据覆盖；重构手写笔盒和文字识别模式。
- **验证证据**：识别笔主链路可跑通；批量文字识别和公式识别请求恢复；分页模式下工具触摸输入问题修复后，识别笔与普通书写工具可区分使用。

## UI 体验、分页和验收材料

- **问了什么**：侧边栏、页面转场、分页视口和验收质量门禁怎样在短时间内补齐到可演示状态？
- **AI 给了什么**：给出按用户可感知优先级分批处理的建议：先保证导航和资料库主流程，再处理动效、分页反馈、性能证据和 AI 使用记录。
- **我改了什么**：补充侧边栏展开收起动画、分组展开动画、页面转场、笔记空态和封面缩略图刷新；改进分页视口、分页进度、添加页提示和滚动条样式；参与整理 Sprint 2 质量门禁、AI 反思日志和安全加固材料。
- **验证证据**：资料库和白板分页交互达到演示要求；Sprint 2 质量门禁记录了 `134 tests passed`、核心模块覆盖率、极端输入场景和性能检查结果；AI 反思日志按验收要求补齐。

## 新建笔记与页面模板

- **问了什么**：如何把「新建笔记」升级为同时承载画布类型、纸张模板、文件导入与标题命名的统一创作起点？
- **AI 给了什么**：拆分笔记类型、模板语义、导入路径与创建交互，并建议复用资料库与白板场景既有链路。
- **我们改了什么**：构建有界/无界、文件导入与空白、横线、格纹、点阵模板的创建流程。
- **验证证据**：创建页覆盖笔记类型、模板和标题输入，创建结果进入资料库与白板的既有场景链路。

## 主题系统与视觉收敛

- **问了什么**：如何在不牺牲信息层级的前提下，将单纯的颜色切换升级为覆盖全局页面的主题系统，并让日间与夜间界面更克制、统一？
- **AI 给了什么**：主题资产、色板、页面覆盖范围与实施顺序的规划，并定位深色预览状态兼容问题。
- **我们改了什么**：补充主题设计与实施计划，收敛主题色和背景使用，复用已有状态控制主题预览。
- **验证证据**：主题资源、主题选择和页面呈现路径形成统一配置，深色预览不再依赖易失的 painter 状态。

## 白板界面与控制层级

- **问了什么**：白板的顶栏、工具栏和全局控制怎样重新分层，才能在不同布局下保持清晰、高效且不挤占画布？
- **AI 给了什么**：基于安全区域、工具栏方向和响应式边界的布局方案，并对控制区位置和尺寸进行复核。
- **我们改了什么**：重构白板顶栏和工具栏层级，将撤回、重做、缩放等全局控制从主工具组中分离，侧边工具栏改为按内容贴顶。
- **验证证据**：顶部与侧边两种布局均有对应控制区位置，工具栏不再因全高约束遮挡或挤压操作项。

## 书写工具交互

- **问了什么**：如何让自由画笔在首次选择、从其他工具恢复、再次调整参数以及快捷键调用时，呈现符合直觉且一致的交互？
- **AI 给了什么**：梳理工具栏与快捷键的共享调用路径，建议以既有 `BrushState` 作为单一状态来源。
- **我们改了什么**：统一工具栏和 `P`/`7` 快捷键逻辑；首次打开笔框，后续恢复完整笔配置，已选中画笔时再次打开笔框。
- **验证证据**：画笔类型、颜色、粗细和压感均由同一状态恢复；点击与快捷键调用进入一致的切换入口。

## 笔记生命周期完整性

- **问了什么**：如何恢复「未产生任何有效内容的新建笔记不应持久化」的原有语义，同时不影响协作场景？
- **AI 给了什么**：通过历史行为和返回调用链定位到系统返回绕过了既有清理逻辑。
- **我们改了什么**：让本地白板系统返回统一进入清理路径；空白新建笔记恢复删除，临时协作房间保持原有行为。
- **验证证据**：系统返回与页面返回复用同一清理入口，空白笔记和临时协作房间按不同生命周期规则处理。

## 鸿蒙基础能力排障

- **问了什么**：当鸿蒙端协作房间创建失败、SQLite FFI 又无法加载系统库时，如何区分服务端、插件和原生链接层的真实故障边界？
- **AI 给了什么**：从日志确认协作接口已成功，问题在房主密钥持久化依赖的安全存储插件；同时审查并否定不可验证的 SQLite 系统库链接方案。
- **我们改了什么**：将协作失败收敛到鸿蒙安全存储适配层；SQLite 保持先验证加载链路、再实施原生适配的策略。
- **验证证据**：创建房间接口返回成功后才进入本地密钥存储步骤；SQLite 问题被定位为系统库加载与构建链接链路，而非业务数据库逻辑。

## path_provider 多平台依赖分治

- **问了什么**：同一插件如何在不同平台走不同的依赖源？如何保证非鸿蒙端自动更新而鸿蒙端锁定社区适配版？
- **AI 给了什么**：vendor化方案——鸿蒙用本地社区版，其他平台保持官方版本自动更新。
- **我们改了什么**：将鸿蒙 path_provider vendor 化，分离平台依赖。
- **验证证据**：Android/iOS/macOS/Web 端 `pub get` 正常拉取官方最新版；鸿蒙端 `flutter build hap --debug` 构建成功且运行时不报插件缺失。

## 笔记/笔记本/标签下拉菜单

- **问了什么**：笔记的移动至和选择标签涉及多对多关系，选择交互如何设计？笔记本/标签删除时内部笔记如何安全回退？
- **AI 给了什么**：`NoteActionsMenu`、`MoveToNotebookDialog`、`SelectTagsDialog` 组件设计。
- **我们改了什么**：按计划实施，修复了弹不出选择框、`Cannot hit test` 错误等问题。
- **验证证据**：重命名后笔记标题即时更新；移动至弹出笔记本列表并成功转移；选择标签支持多选且"未标签"互斥逻辑正确；删除后笔记出现在回收站。

## 长按与点击的菜单统一

- **问了什么**：长按和点击小箭头触发的下拉菜单如何复用同一套定位逻辑，避免两套实现产生位置漂移？
- **AI 给了什么**：复用同一个回调函数，统一弹出位置。
- **我们改了什么**：让长按和点击走同一套菜单逻辑，位置一致。
- **验证证据**：长按封面和点击小箭头弹出的菜单位置一致，均位于目标元素正下方；菜单项功能完全相同。

## 笔记本/标签创建与封面选择

- **问了什么**：创建页面如何在同一视图内同时承载预览、信息输入和封面模板选择三个功能区域？
- **AI 给了什么**：封面选择页面布局方案——左上预览图、左下输入框和颜色选择、右侧封面列表。
- **我们改了什么**：多次重构排版，反复调整键盘弹出时的布局适配。
- **验证证据**：预览图实时反映所选封面和标题；选择封面后纯色预览不再叠加笔记本/#标记；最近使用封面记录按笔记本/标签分别存储。

## 键盘弹出的布局自适应

- **问了什么**：平板端键盘弹出时如何避免布局溢出和视觉闪烁？页面位移如何与键盘动画同步？
- **AI 给了什么**：`TweenAnimationBuilder` 跟随 viewInsets 变化的动画方案。
- **我们改了什么**：多次迭代键盘覆盖方式和动画速度，最终改为默认高度为收缩后高度。
- **验证证据**：键盘弹出时输入框平滑上移不被遮挡，无 `RenderFlex overflowed` 报错；键盘收起时页面平滑回落无闪烁。

## 导航栏切换动画

- **问了什么**：SegmentedButton 切换时为什么会触发布局重排？如何让指示器跟随手势平滑过渡而非整段跳变？
- **AI 给了什么**：短横线跟随手指滑动方案；点击按钮时短横线滑动（非瞬移）方案。
- **我们改了什么**：实现短横线平滑跟随、文字颜色在默认色和选中色之间渐变。
- **验证证据**：页面切换动画测试。

## 新建笔记页面

- **问了什么**：有界/无界笔记的数据模型差异如何体现在创建流程中？页面模板选择如何与笔记类型联动？
- **AI 给了什么**：建议采用 dialog 形式承载创建流程，左上预览图 + 右侧选择区，笔记类型与模板分离但在同一视图内联动选择，创建按钮带背景色填充以区分取消操作。
- **我们改了什么**：实现新建笔记页面，包含取消/创建按钮、模板选择。
- **验证证据**：选择有界/无界笔记后创建的笔记画布行为不同；空白、窄横线、宽横线、格纹、点阵五种模板正确应用到新笔记；创建后自动跳转至白板编辑页。

---

## 反思结论

1. **AI 的核心价值是定位与拆解**。AI 最有价值的部分是快速定位跨平台调用链，并把问题拆成可验证假设。
2. **人工必须决定改动边界**。协作协议、数据库和跨端适配等关键路径，不能直接实现未经抓包、测试或真机验证的推断。
3. **书写与协作要联合回归**。本 Sprint 曾出现“改善局部书写体验但破坏协作”的回归，后续编辑器输入改动必须同时执行书写与协作回归测试。
4. **原生改动必须全量重编**。跨平台原生改动（鸿蒙 `.ets`、安卓 `Kotlin`）热重载不生效；AI 给出的 API 命名空间要查官方文档核对（如鸿蒙 `PhotoViewPicker` 属 `@kit.MediaLibraryKit` 而非 `@kit.CoreFileKit` 的 `picker`）。
5. **选型先验准入门槛**。功能选型要先验证“准入门槛”再写代码。网盘备份先否决百度网盘 OAuth 路径、确认坚果云 WebDAV 无准入门槛后，才进入实施，避免返工。
6. **方案对比要落到源码**。多 AI 方案对比不能只看文档措辞，要落到源码级证据。对比三份协作研究文档时，最终靠 `socket_io_client 3.1.6` 源码确认 CSR 的 pid/offset/recovered 均已实现，推翻了“Dart 不支持 CSR”的初始判断——grep 返回零匹配不代表能力不存在，可能是搜索方式有误。
7. **计划审计阻断阻塞**。Phase 0 计划在 5 轮审查中累计发现 5 个阻塞问题（syncAll→sceneInit 误传 bypass、undo/redo 未写回控制器、onSceneChanged 签名传播不全、version 表缺 versionNonce、快照未等 in-flight），全部在编码前修复，避免了“实现完再返工”的高成本。
8. **平台差异先排代码再查网络**。安卓创建房间 15s 超时而鸿蒙正常，最终定位为端口封锁而非 Flutter HTTP 客户端缺陷——`encrypted_scene_store.dart` 在 Phase 0 零改动是排除代码问题的关键证据。
9. **交互改动必须先定义状态语义**。画笔选择、专注模式退出和工具栏切换不是单纯的 UI 调整；应先明确首次进入、恢复上次状态、再次触发与快捷键等状态转换，再让所有入口复用同一逻辑。
10. **创作对象要按有效生命周期管理**。新建笔记的模板、标题和画布类型应在创建阶段一次确定；未产生有效内容的临时笔记应在离开时清理，避免无意义数据进入资料库。
